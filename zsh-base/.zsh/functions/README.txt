This directory contains zsh function scripts.  These scripts are
in the fpath and are therefore autoloaded by zsh when you call them.

Place the definition of the function in the file.  The files only need
to be readable, not executable to work correctly.

